<% flash.each do |type, message| %>
  <div class="mb-4 rounded-lg px-4 py-3 text-sm <%= type.to_s == "alert" ? "bg-red-50 text-red-800" : "bg-green-50 text-green-800" %>">
    <%= message %>
  </div>
<% end %>
